import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'spot-check-ai',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
